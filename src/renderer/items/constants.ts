// keep them here,
// so we know it's the same for all

export const maxVelocity: number = 5
export const minVelocity: number = 3

// on edges
export const turnFactor: number = 0.1

// Separation
export const avoidFactor = 0.05
export const protectedRange = 16

// Alignment
export const matchingfactor = 0.15

// Cohesion
export const centeringFactor = 0.0005

// Predator
export const predatorturnfactor = 0.9


